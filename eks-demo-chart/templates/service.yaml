apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-demo-chart.fullname" . }}
  labels:
    {{- include "eks-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-demo-chart.selectorLabels" . | nindent 4 }}
